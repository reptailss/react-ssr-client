import { ReactNode } from 'react';
import { AppLocation } from '@appLocation/types';
export declare const AppLocationProvider: ({ initial, children, }: {
    initial: AppLocation;
    children: ReactNode;
}) => import("react/jsx-runtime").JSX.Element;
